= form_for @sandbox do |f|
  - if @sandbox.errors.any?
    #error_explanation
      %h2= "#{pluralize(@sandbox.errors.count, "error")} prohibited this sandbox from being saved:"
      %ul
        - @sandbox.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
